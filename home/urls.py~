
from django.urls import path
from django.views.generic import TemplateView
from django.shortcuts import render_to_response

app_name = 'home'
#urlpatterns = [path('', TemplateView.as_view(template_name='home/home.html'))]
urlpatterns = [path('', views.OverView.as_view(), name='home')]
